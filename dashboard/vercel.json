{
    "buildCommand": "cd ../sdk && npm install --ignore-scripts && npm run build && cd ../dashboard && npm install && npm run build",
    "outputDirectory": "dist",
    "framework": "vite",
    "rewrites": [
        {
            "source": "/(.*)",
            "destination": "/index.html"
        }
    ]
}