{
    "buildCommand": "cd ../sdk && npm install --omit=dev --ignore-scripts && cd ../playground && npm install && npm run build",
    "outputDirectory": "dist",
    "framework": "vite",
    "rewrites": [
        {
            "source": "/(.*)",
            "destination": "/index.html"
        }
    ]
}