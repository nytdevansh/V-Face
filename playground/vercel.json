{
    "buildCommand": "cd ../sdk && npm install --ignore-scripts && npm run build && cd ../playground && npm install && npm run build",
    "outputDirectory": "dist",
    "framework": "vite",
    "rewrites": [
        {
            "source": "/(.*)",
            "destination": "/index.html"
        }
    ]
}