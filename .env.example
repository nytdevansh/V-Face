# Your wallet private key (NEVER commit the actual .env file!)
# Get testnet MATIC from: https://faucet.polygon.technology/
PRIVATE_KEY=your_wallet_private_key_here

# RPC URLs (these are public, you can use them or get your own from Alchemy/Infura)
MUMBAI_RPC_URL=https://rpc-mumbai.maticvigil.com
POLYGON_RPC_URL=https://polygon-rpc.com

# For contract verification (get from https://polygonscan.com/apis)
POLYGONSCAN_API_KEY=your_polygonscan_api_key_here

# V-Face Registry Server
VFACE_REGISTRY_URL=http://localhost:3000

# Embedding encryption key (32-byte hex). Generate with: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
# If not set, a dev key is derived automatically (NOT safe for production!)
VFACE_ENCRYPTION_KEY=
